#include <stdio.h>
#include "main.h"

/**
 * main - function that prints promgram's name
 * @argc: arguments to be made
 * @argv: arguments arrays
 * Return: is always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);

return (0);
}

